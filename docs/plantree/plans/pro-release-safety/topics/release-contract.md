# Release Contract

Date: 2026-09-03

发布分为验证、清单、镜像和部署四阶段。前三阶段不能连接生产；镜像产出不能自动触发部署。部署只接受 Pro GHCR 摘要，并在 SSH 端再次核对镜像标签、容器、数据库、域名、备份和二开标识。

无迁移时，应用验证失败可恢复旧镜像。有迁移时，发布需要显式授权且禁止自动应用回退，恢复必须按升级前数据库备份执行。盈利 sidecar 是独立部署组件，只验证路由，不随 app 重建。
